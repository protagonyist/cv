## CV

My CV in LaTeX, using template by Jan Vorisek https://github.com/janvorisek/minimal-latex-cv  

Commands:
```bash
# render main.pdf
pdflatex -synctex=1 -interaction=nonstopmode main.tex
# clean all files that match .gitignore (build artifacts)
git clean -Xf
```  
